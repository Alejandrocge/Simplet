<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme to avoid pulling AppCompat/Material resources into the proof build. -->
    <style name="Theme.Simplet" parent="@android:style/Theme.Material.NoActionBar" />
</resources>
